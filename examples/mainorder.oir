module mainorder
use ui
form win
  title = "order"
  label greeting
    text = "before"
  end
end
sub main
  greeting.text = "set from main"
  call print_text(concat("main saw: ", greeting.text))
end
