# win, user32 — the USER32.DLL half of the Win32 kit: windows, messages,
# dialogs, input, hooks, the clipboard, menus, and the painting calls that
# live in user32 rather than gdi32 (BeginPaint, FillRect, DrawTextA, GetDC).
#
# Every entry that takes or answers a string is the ANSI (`...A`) one, and
# every line is reached with the `system` convention. Windows only — the kit's
# lib.json gates it to `"windows"`.
#
# Reading the transcriptions:
#
#   - A handle (HWND, HINSTANCE, HMENU, HDC, HICON, HCURSOR, HBRUSH, HHOOK,
#     HGLOBAL, LPVOID, and any struct pointer that may be NULL) is `ptr`.
#     `ptr_null()` is the NULL a C caller writes.
#   - WPARAM, LPARAM and LRESULT are integer payloads, so they are `int64`.
#     A handle that travels *in* a WPARAM/LPARAM is `ptr_to_int(h)` at the
#     call site, and `ptr_from_int(v)` coming back.
#   - DWORD, UINT, BOOL, int and ATOM are `int`. An ATOM and a SHORT return
#     only define their low 16 bits; test the bits you want rather than the
#     sign of the whole word.
#   - LPCSTR is `text` where the API always wants a string. Where NULL is the
#     ordinary call (FindWindowA's second argument) or the argument is really
#     a MAKEINTRESOURCE number (LoadCursorA, LoadIconA), the parameter is
#     `ptr` and a `...NameA`/`...ByNameA` sibling under `as` gives the string
#     form. Pass `ptr_from_int(IDC_ARROW)` for a resource id — a bare constant
#     is an integer literal and fits `ptr_from_int`'s `int64` directly, but an
#     expression built from constants is an `int`, so that one is spelled
#     `ptr_from_int(int_to_int64(COLOR_WINDOW + 1))`. The same widening goes
#     the other way for a `size of` handed to an `int` parameter:
#     `int64_to_int(size of INPUT)`.
#   - A struct that the callee fills in is declared as its c-record, so the
#     record is handed over as a pointer automatically. A struct pointer that
#     is *optionally* NULL (InvalidateRect's rect) stays `ptr`, so
#     `address of r` or `ptr_null()` both fit.
#   - The HWND_* and GWLP_* constants are numbers used as pointers or as
#     signed indices: `ptr_from_int(HWND_MESSAGE)`, `GWLP_USERDATA` as is.
#
# Not expressible here, and why:
#
#   - WindowFromPoint and ChildWindowFromPoint take a POINT *by value*. A
#     c-record parameter is always a pointer, so they are left out rather
#     than transcribed wrongly.
#   - INPUT is a union. Its two useful arms are declared as two records that
#     both measure 40 bytes — the real sizeof(INPUT) — so SendInput's `size`
#     argument is right whichever arm is filled in.

# ---------------------------------------------------------------- structures

# POINT, RECT, SIZE and PAINTSTRUCT are windef.h shapes that belong to no one
# DLL. This kit declares them once, in `gdi32.oed`, and every signature below
# that names one uses that declaration — a kit's records share one namespace,
# so there is exactly one RECT in `use win`.
#
# For reference, the fields those four carry:
#   POINT       x, y
#   RECT        left, top, right, bottom
#   SIZE        cx, cy
#   PAINTSTRUCT hdc, erase, paint, restore, inc_update, reserved
#
# GetDC, GetWindowDC, ReleaseDC, BeginPaint, EndPaint and FillRect are user32
# exports that `gdi32.oed` declares, because they are only ever called beside
# the GDI drawing calls. They are not repeated here.

# MSG — 48 bytes: hwnd@0, message@8, wparam@16, lparam@24, time@32, pt@36.
record MSG is c
  hwnd: ptr
  message: int
  wparam: int64
  lparam: int64
  time: int
  pt: POINT
end

# WNDCLASSEXA — 80 bytes. `cb_size` must be set to `size of WNDCLASSEXA`
# before RegisterClassExA, which is how the API versions the struct.
# `wnd_proc` is `address of` a sub shaped like a WNDPROC:
#   sub wndproc(hwnd: ptr, msg: int, wparam: int64, lparam: int64): int64 system
# `background` is conventionally
# `ptr_from_int(int_to_int64(COLOR_WINDOW + 1))`.
record WNDCLASSEXA is c
  cb_size: int
  style: int
  wnd_proc: ptr
  cls_extra: int
  wnd_extra: int
  instance: ptr
  icon: ptr
  cursor: ptr
  background: ptr
  menu_name: text
  class_name: text
  icon_sm: ptr
end

# WNDCLASSA — the older, 72-byte form RegisterClassA takes. No cbSize, no
# small icon.
record WNDCLASSA is c
  style: int
  wnd_proc: ptr
  cls_extra: int
  wnd_extra: int
  instance: ptr
  icon: ptr
  cursor: ptr
  background: ptr
  menu_name: text
  class_name: text
end

# CREATESTRUCTA — 80 bytes. WM_NCCREATE and WM_CREATE arrive with its address
# in LPARAM: `var cs: CREATESTRUCTA` cannot receive that, so read it through
# `ptr_from_int(lparam)` and the offsets, or copy it with mem_copy.
record CREATESTRUCTA is c
  create_params: ptr
  instance: ptr
  menu: ptr
  parent: ptr
  cy: int
  cx: int
  y: int
  x: int
  style: int
  name: text
  class_name: text
  ex_style: int
end

# MINMAXINFO — 40 bytes, the struct WM_GETMINMAXINFO points at.
record MINMAXINFO is c
  reserved: POINT
  max_size: POINT
  max_position: POINT
  min_track_size: POINT
  max_track_size: POINT
end

# WINDOWPLACEMENT — 44 bytes. `length` must be `size of WINDOWPLACEMENT`.
record WINDOWPLACEMENT is c
  length: int
  flags: int
  show_cmd: int
  min_position: POINT
  max_position: POINT
  normal_position: RECT
end

# WINDOWPOS — 40 bytes, what WM_WINDOWPOSCHANGING/CHANGED point at.
record WINDOWPOS is c
  hwnd: ptr
  insert_after: ptr
  x: int
  y: int
  cx: int
  cy: int
  flags: int
end

# KBDLLHOOKSTRUCT — 24 bytes. LPARAM of a WH_KEYBOARD_LL hook points at one.
record KBDLLHOOKSTRUCT is c
  vk_code: int
  scan_code: int
  flags: int
  time: int
  extra_info: int64
end

# MSLLHOOKSTRUCT — 32 bytes: pt@0, mouse_data@8, flags@12, time@16,
# extra_info@24. LPARAM of a WH_MOUSE_LL hook points at one.
record MSLLHOOKSTRUCT is c
  pt: POINT
  mouse_data: int
  flags: int
  time: int
  extra_info: int64
end

# CWPSTRUCT — 32 bytes, the LPARAM of a WH_CALLWNDPROC hook.
record CWPSTRUCT is c
  lparam: int64
  wparam: int64
  message: int
  hwnd: ptr
end

record MOUSEINPUT is c
  dx: int
  dy: int
  mouse_data: int
  flags: int
  time: int
  extra_info: int64
end

record KEYBDINPUT is c
  vk: word
  scan: word
  flags: int
  time: int
  extra_info: int64
end

# INPUT with the KEYBDINPUT arm of its union selected. `union_tail` is the
# rest of the union — MOUSEINPUT is the widest arm — so this record is the
# real 40-byte sizeof(INPUT) that SendInput's third argument must be given.
# Set `kind` to INPUT_KEYBOARD.
record INPUT is c
  kind: int
  ki: KEYBDINPUT
  union_tail: byte[8]
end

# The same 40-byte INPUT with the MOUSEINPUT arm selected. Set `kind` to
# INPUT_MOUSE. Both records are `size of` 40; an array of either is a valid
# SendInput batch of that one kind.
record INPUTMOUSE is c
  kind: int
  mi: MOUSEINPUT
end

# ------------------------------------------------------------------- windows

dll CreateWindowExA(ex_style: int, class_name: text, window_name: text, style: int, x: int, y: int, width: int, height: int, parent: ptr, menu: ptr, instance: ptr, param: ptr): ptr from "user32" as "CreateWindowExA" system
# The same call with the class given as an ATOM (the RegisterClassExA result,
# through ptr_from_int) and no window text.
dll CreateWindowExAtomA(ex_style: int, class_atom: ptr, window_name: ptr, style: int, x: int, y: int, width: int, height: int, parent: ptr, menu: ptr, instance: ptr, param: ptr): ptr from "user32" as "CreateWindowExA" system

# RegisterClassExA answers an ATOM: nonzero on success, and only its low 16
# bits are defined.
dll RegisterClassExA(wc: WNDCLASSEXA): int from "user32" system
dll RegisterClassA(wc: WNDCLASSA): int from "user32" system
dll UnregisterClassA(class_name: text, instance: ptr): bool from "user32" system
dll GetClassNameA(hwnd: ptr, buffer: ptr, max_count: int): int from "user32" system
dll GetClassLongPtrA(hwnd: ptr, index: int): int64 from "user32" system
dll SetClassLongPtrA(hwnd: ptr, index: int, value: int64): int64 from "user32" system

dll DefWindowProcA(hwnd: ptr, msg: int, wparam: int64, lparam: int64): int64 from "user32" system
dll CallWindowProcA(prev_proc: ptr, hwnd: ptr, msg: int, wparam: int64, lparam: int64): int64 from "user32" system

dll ShowWindow(hwnd: ptr, cmd: int): bool from "user32" system
dll ShowWindowAsync(hwnd: ptr, cmd: int): bool from "user32" system
dll UpdateWindow(hwnd: ptr): bool from "user32" system
dll DestroyWindow(hwnd: ptr): bool from "user32" system
dll CloseWindow(hwnd: ptr): bool from "user32" system
dll IsWindow(hwnd: ptr): bool from "user32" system
dll IsWindowVisible(hwnd: ptr): bool from "user32" system
dll IsWindowEnabled(hwnd: ptr): bool from "user32" system
dll IsIconic(hwnd: ptr): bool from "user32" system
dll IsZoomed(hwnd: ptr): bool from "user32" system
dll EnableWindow(hwnd: ptr, enable: bool): bool from "user32" system
dll AnimateWindow(hwnd: ptr, time: int, flags: int): bool from "user32" system
dll FlashWindow(hwnd: ptr, invert: bool): bool from "user32" system

dll GetClientRect(hwnd: ptr, rect: RECT): bool from "user32" system
dll GetWindowRect(hwnd: ptr, rect: RECT): bool from "user32" system
dll AdjustWindowRect(rect: RECT, style: int, has_menu: bool): bool from "user32" system
dll AdjustWindowRectEx(rect: RECT, style: int, has_menu: bool, ex_style: int): bool from "user32" system
dll MoveWindow(hwnd: ptr, x: int, y: int, width: int, height: int, repaint: bool): bool from "user32" system
dll SetWindowPos(hwnd: ptr, insert_after: ptr, x: int, y: int, cx: int, cy: int, flags: int): bool from "user32" system
dll GetWindowPlacement(hwnd: ptr, placement: WINDOWPLACEMENT): bool from "user32" system
dll SetWindowPlacement(hwnd: ptr, placement: WINDOWPLACEMENT): bool from "user32" system
dll BringWindowToTop(hwnd: ptr): bool from "user32" system

dll SetWindowTextA(hwnd: ptr, title: text): bool from "user32" system
dll GetWindowTextA(hwnd: ptr, buffer: ptr, max_count: int): int from "user32" system
dll GetWindowTextLengthA(hwnd: ptr): int from "user32" system

# The pointer-width window words. On x64 these are real user32 exports, not
# the macros they are in a 32-bit build. GWLP_USERDATA is where a program
# stashes its own state for a WNDPROC to find.
dll GetWindowLongPtrA(hwnd: ptr, index: int): int64 from "user32" system
dll SetWindowLongPtrA(hwnd: ptr, index: int, value: int64): int64 from "user32" system
dll GetWindowLongA(hwnd: ptr, index: int): int from "user32" system
dll SetWindowLongA(hwnd: ptr, index: int, value: int): int from "user32" system

dll FindWindowA(class_name: text, window_name: text): ptr from "user32" system
# The usual call: a class name and a NULL title. Pass ptr_null() for `title`.
dll FindWindowClassA(class_name: text, title: ptr): ptr from "user32" as "FindWindowA" system
dll FindWindowExA(parent: ptr, child_after: ptr, class_name: text, window_name: text): ptr from "user32" system

dll GetParent(hwnd: ptr): ptr from "user32" system
dll SetParent(child: ptr, new_parent: ptr): ptr from "user32" system
dll GetWindow(hwnd: ptr, cmd: int): ptr from "user32" system
dll GetTopWindow(hwnd: ptr): ptr from "user32" system
dll GetDesktopWindow(): ptr from "user32" system
dll GetShellWindow(): ptr from "user32" system
dll GetForegroundWindow(): ptr from "user32" system
dll SetForegroundWindow(hwnd: ptr): bool from "user32" system
dll GetActiveWindow(): ptr from "user32" system
dll SetActiveWindow(hwnd: ptr): ptr from "user32" system
dll GetFocus(): ptr from "user32" system
dll SetFocus(hwnd: ptr): ptr from "user32" system

# The callback is `address of` a sub shaped
#   sub each(hwnd: ptr, param: int64): bool system
dll EnumWindows(callback: ptr, param: int64): bool from "user32" system
dll EnumChildWindows(parent: ptr, callback: ptr, param: int64): bool from "user32" system
dll EnumThreadWindows(thread_id: int, callback: ptr, param: int64): bool from "user32" system
dll GetWindowThreadProcessId(hwnd: ptr, process_id: ptr): int from "user32" system

dll ScreenToClient(hwnd: ptr, point: POINT): bool from "user32" system
dll ClientToScreen(hwnd: ptr, point: POINT): bool from "user32" system
dll GetCursorPos(point: POINT): bool from "user32" system
dll SetCursorPos(x: int, y: int): bool from "user32" system

# ------------------------------------------------------------------ messages

# GetMessageA answers 1 for a message, 0 for WM_QUIT, and -1 for an error —
# so the loop condition is `> 0`, never a plain truth test.
dll GetMessageA(msg: MSG, hwnd: ptr, filter_min: int, filter_max: int): int from "user32" system
dll PeekMessageA(msg: MSG, hwnd: ptr, filter_min: int, filter_max: int, remove_flag: int): bool from "user32" system
dll TranslateMessage(msg: MSG): bool from "user32" system
dll DispatchMessageA(msg: MSG): int64 from "user32" system
dll WaitMessage(): bool from "user32" system
dll PostQuitMessage(exit_code: int) from "user32" system

dll SendMessageA(hwnd: ptr, msg: int, wparam: int64, lparam: int64): int64 from "user32" system
dll SendMessageTimeoutA(hwnd: ptr, msg: int, wparam: int64, lparam: int64, flags: int, timeout: int, result: ptr): int64 from "user32" system
dll SendNotifyMessageA(hwnd: ptr, msg: int, wparam: int64, lparam: int64): bool from "user32" system
dll PostMessageA(hwnd: ptr, msg: int, wparam: int64, lparam: int64): bool from "user32" system
dll PostThreadMessageA(thread_id: int, msg: int, wparam: int64, lparam: int64): bool from "user32" system
dll ReplyMessage(result: int64): bool from "user32" system
dll InSendMessage(): bool from "user32" system

dll GetMessagePos(): int from "user32" system
dll GetMessageTime(): int from "user32" system
dll RegisterWindowMessageA(name: text): int from "user32" system

# SetTimer's id and result are UINT_PTR. With a NULL hwnd the return is the
# timer id to pass back to KillTimer; with a window it is the id given.
dll SetTimer(hwnd: ptr, id: int64, elapse_ms: int, callback: ptr): int64 from "user32" system
dll KillTimer(hwnd: ptr, id: int64): bool from "user32" system

# ------------------------------------------------------------ dialogs & misc

dll MessageBoxA(owner: ptr, body: text, caption: text, kind: int): int from "user32" system
dll MessageBoxExA(owner: ptr, body: text, caption: text, kind: int, language_id: int): int from "user32" system
dll MessageBeep(kind: int): bool from "user32" system

# `name` is a MAKEINTRESOURCE number: ptr_from_int(IDC_ARROW). The `...NameA`
# sibling is the same export reached with a string resource name instead.
dll LoadCursorA(instance: ptr, name: ptr): ptr from "user32" system
dll LoadCursorNameA(instance: ptr, name: text): ptr from "user32" as "LoadCursorA" system
dll LoadIconA(instance: ptr, name: ptr): ptr from "user32" system
dll LoadIconNameA(instance: ptr, name: text): ptr from "user32" as "LoadIconA" system
dll LoadImageA(instance: ptr, name: ptr, kind: int, cx: int, cy: int, flags: int): ptr from "user32" system
dll LoadImageFileA(instance: ptr, path: text, kind: int, cx: int, cy: int, flags: int): ptr from "user32" as "LoadImageA" system
dll DestroyIcon(icon: ptr): bool from "user32" system
dll DestroyCursor(cursor: ptr): bool from "user32" system
dll SetCursor(cursor: ptr): ptr from "user32" system
dll ShowCursor(visible: bool): int from "user32" system

dll GetSystemMetrics(index: int): int from "user32" system
dll SystemParametersInfoA(action: int, param: int, data: ptr, win_ini: int): bool from "user32" system
dll GetSysColor(index: int): int from "user32" system
dll GetSysColorBrush(index: int): ptr from "user32" system
dll GetDoubleClickTime(): int from "user32" system

dll GetDlgItem(dialog: ptr, item_id: int): ptr from "user32" system
dll GetDlgCtrlID(hwnd: ptr): int from "user32" system
dll SetDlgItemTextA(dialog: ptr, item_id: int, title: text): bool from "user32" system
dll GetDlgItemTextA(dialog: ptr, item_id: int, buffer: ptr, max_count: int): int from "user32" system
dll SetDlgItemInt(dialog: ptr, item_id: int, value: int, signed_flag: bool): bool from "user32" system
dll GetDlgItemInt(dialog: ptr, item_id: int, translated: ptr, signed_flag: bool): int from "user32" system
dll EndDialog(dialog: ptr, result: int64): bool from "user32" system

# --------------------------------------------------------------------- input

# GetAsyncKeyState and GetKeyState answer a SHORT. Test the 32768 bit for
# "down" and the 1 bit for "toggled"; do not test the sign of the int.
dll GetAsyncKeyState(key: int): int from "user32" system
dll GetKeyState(key: int): int from "user32" system
dll GetKeyboardState(state: ptr): bool from "user32" system
dll SetKeyboardState(state: ptr): bool from "user32" system
dll GetKeyboardLayout(thread_id: int): ptr from "user32" system
dll MapVirtualKeyA(code: int, map_type: int): int from "user32" system
dll VkKeyScanA(character: int): int from "user32" system
dll GetKeyNameTextA(lparam: int, buffer: ptr, size: int): int from "user32" system

# The hook procedure is `address of` a sub shaped
#   sub hook(code: int, wparam: int64, lparam: int64): int64 system
# which returns CallNextHookEx(...) for anything it does not handle. A
# WH_KEYBOARD_LL or WH_MOUSE_LL hook is global: `hmod` may be NULL and
# `thread_id` 0, and the thread that installs it must pump messages.
dll SetWindowsHookExA(hook_id: int, hook_proc: ptr, hmod: ptr, thread_id: int): ptr from "user32" system
dll CallNextHookEx(hook: ptr, code: int, wparam: int64, lparam: int64): int64 from "user32" system
dll UnhookWindowsHookEx(hook: ptr): bool from "user32" system

dll RegisterHotKey(hwnd: ptr, hotkey_id: int, modifiers: int, key: int): bool from "user32" system
dll UnregisterHotKey(hwnd: ptr, hotkey_id: int): bool from "user32" system

# `inputs` is `address of` an INPUT (or the first of an array of them) and
# `size` is `int64_to_int(size of INPUT)` — 40, both arms.
dll SendInput(count: int, inputs: ptr, size: int): int from "user32" system
# The legacy pair. Their first arguments are BYTEs in C; an `int` argument
# arrives in the same register and the callee reads its low byte.
dll keybd_event(vk: int, scan: int, flags: int, extra_info: int64) from "user32" system
dll mouse_event(flags: int, dx: int, dy: int, data: int, extra_info: int64) from "user32" system

dll SetCapture(hwnd: ptr): ptr from "user32" system
dll ReleaseCapture(): bool from "user32" system
dll GetCapture(): ptr from "user32" system

# ----------------------------------------------------------------- clipboard

dll OpenClipboard(owner: ptr): bool from "user32" system
dll CloseClipboard(): bool from "user32" system
dll EmptyClipboard(): bool from "user32" system
# The handle GetClipboardData answers belongs to the clipboard; lock it with
# kernel32's GlobalLock to read the bytes, and do not free it. The handle
# handed to SetClipboardData becomes the clipboard's.
dll GetClipboardData(format: int): ptr from "user32" system
dll SetClipboardData(format: int, handle: ptr): ptr from "user32" system
dll IsClipboardFormatAvailable(format: int): bool from "user32" system
dll EnumClipboardFormats(format: int): int from "user32" system
dll CountClipboardFormats(): int from "user32" system
dll RegisterClipboardFormatA(name: text): int from "user32" system
dll GetClipboardFormatNameA(format: int, buffer: ptr, max_count: int): int from "user32" system
dll GetClipboardOwner(): ptr from "user32" system
dll GetOpenClipboardWindow(): ptr from "user32" system
dll GetClipboardSequenceNumber(): int from "user32" system

# ------------------------------------------------------- painting (in user32)

# BeginPaint, EndPaint, GetDC, GetWindowDC, ReleaseDC and FillRect are in
# `gdi32.oed`, beside the drawing calls they are always written with.

# `rect` is optional here: `address of` a RECT, or ptr_null() for the whole
# window.
dll InvalidateRect(hwnd: ptr, rect: ptr, erase: bool): bool from "user32" system
dll ValidateRect(hwnd: ptr, rect: ptr): bool from "user32" system
dll FrameRect(dc: ptr, rect: RECT, brush: ptr): int from "user32" system
dll InvertRect(dc: ptr, rect: RECT): bool from "user32" system
# `count` of -1 means "to the NUL".
dll DrawTextA(dc: ptr, body: text, count: int, rect: RECT, format: int): int from "user32" system
dll DrawFocusRect(dc: ptr, rect: RECT): bool from "user32" system
dll SetRect(rect: RECT, left: int, top: int, right: int, bottom: int): bool from "user32" system
dll SetRectEmpty(rect: RECT): bool from "user32" system
dll IsRectEmpty(rect: RECT): bool from "user32" system
dll OffsetRect(rect: RECT, dx: int, dy: int): bool from "user32" system
dll InflateRect(rect: RECT, dx: int, dy: int): bool from "user32" system
# PtInRect is left out: C spells it `PtInRect(const RECT *, POINT)`, and the
# POINT is by value, which a c-record parameter can never be. Write the four
# comparisons — `x >= r.left and x < r.right and y >= r.top and y < r.bottom`
# — which is what PtInRect does.

# --------------------------------------------------------------------- menus

dll CreateMenu(): ptr from "user32" system
dll CreatePopupMenu(): ptr from "user32" system
dll DestroyMenu(menu: ptr): bool from "user32" system
dll AppendMenuA(menu: ptr, flags: int, item_id: int64, item: text): bool from "user32" system
dll AppendSubMenuA(menu: ptr, flags: int, submenu: ptr, item: text): bool from "user32" as "AppendMenuA" system
dll InsertMenuA(menu: ptr, position: int, flags: int, item_id: int64, item: text): bool from "user32" system
dll DeleteMenu(menu: ptr, position: int, flags: int): bool from "user32" system
dll ModifyMenuA(menu: ptr, position: int, flags: int, item_id: int64, item: text): bool from "user32" system
dll EnableMenuItem(menu: ptr, item_id: int, enable: int): int from "user32" system
dll CheckMenuItem(menu: ptr, item_id: int, check: int): int from "user32" system
dll GetMenu(hwnd: ptr): ptr from "user32" system
dll SetMenu(hwnd: ptr, menu: ptr): bool from "user32" system
dll GetSubMenu(menu: ptr, position: int): ptr from "user32" system
dll GetMenuItemCount(menu: ptr): int from "user32" system
dll GetSystemMenu(hwnd: ptr, revert: bool): ptr from "user32" system
dll DrawMenuBar(hwnd: ptr): bool from "user32" system
dll TrackPopupMenu(menu: ptr, flags: int, x: int, y: int, reserved: int, hwnd: ptr, rect: ptr): bool from "user32" system

# ----------------------------------------------------------------- WM_ values

const WM_NULL = 0
const WM_CREATE = 1
const WM_DESTROY = 2
const WM_MOVE = 3
const WM_SIZE = 5
const WM_ACTIVATE = 6
const WM_SETFOCUS = 7
const WM_KILLFOCUS = 8
const WM_ENABLE = 10
const WM_SETREDRAW = 11
const WM_SETTEXT = 12
const WM_GETTEXT = 13
const WM_GETTEXTLENGTH = 14
const WM_PAINT = 15
const WM_CLOSE = 16
const WM_QUERYENDSESSION = 17
const WM_QUIT = 18
const WM_ERASEBKGND = 20
const WM_SYSCOLORCHANGE = 21
const WM_SHOWWINDOW = 24
const WM_ACTIVATEAPP = 28
const WM_SETCURSOR = 32
const WM_MOUSEACTIVATE = 33
const WM_GETMINMAXINFO = 36
const WM_WINDOWPOSCHANGING = 70
const WM_WINDOWPOSCHANGED = 71
const WM_NOTIFY = 78
const WM_NCCREATE = 129
const WM_NCDESTROY = 130
const WM_NCCALCSIZE = 131
const WM_NCHITTEST = 132
const WM_NCPAINT = 133
const WM_GETDLGCODE = 135
const WM_KEYDOWN = 256
const WM_KEYUP = 257
const WM_CHAR = 258
const WM_DEADCHAR = 259
const WM_SYSKEYDOWN = 260
const WM_SYSKEYUP = 261
const WM_SYSCHAR = 262
const WM_INITDIALOG = 272
const WM_COMMAND = 273
const WM_SYSCOMMAND = 274
const WM_TIMER = 275
const WM_HSCROLL = 276
const WM_VSCROLL = 277
const WM_INITMENU = 278
const WM_INITMENUPOPUP = 279
const WM_MENUSELECT = 287
const WM_MENUCHAR = 288
const WM_CTLCOLOREDIT = 307
const WM_CTLCOLORLISTBOX = 308
const WM_CTLCOLORBTN = 309
const WM_CTLCOLORDLG = 310
const WM_CTLCOLORSTATIC = 312
const WM_MOUSEMOVE = 512
const WM_LBUTTONDOWN = 513
const WM_LBUTTONUP = 514
const WM_LBUTTONDBLCLK = 515
const WM_RBUTTONDOWN = 516
const WM_RBUTTONUP = 517
const WM_RBUTTONDBLCLK = 518
const WM_MBUTTONDOWN = 519
const WM_MBUTTONUP = 520
const WM_MBUTTONDBLCLK = 521
const WM_MOUSEWHEEL = 522
const WM_XBUTTONDOWN = 523
const WM_XBUTTONUP = 524
const WM_MOUSEHWHEEL = 526
const WM_ENTERSIZEMOVE = 561
const WM_EXITSIZEMOVE = 562
const WM_DROPFILES = 563
const WM_DPICHANGED = 736
const WM_HOTKEY = 786
const WM_CLIPBOARDUPDATE = 797
const WM_USER = 1024
const WM_APP = 32768

# ----------------------------------------------------------------- WS_ values

const WS_OVERLAPPED = 0
const WS_TABSTOP = 65536
const WS_MAXIMIZEBOX = 65536
const WS_GROUP = 131072
const WS_MINIMIZEBOX = 131072
const WS_THICKFRAME = 262144
const WS_SYSMENU = 524288
const WS_HSCROLL = 1048576
const WS_VSCROLL = 2097152
const WS_DLGFRAME = 4194304
const WS_BORDER = 8388608
const WS_CAPTION = 12582912
const WS_MAXIMIZE = 16777216
const WS_CLIPCHILDREN = 33554432
const WS_CLIPSIBLINGS = 67108864
const WS_DISABLED = 134217728
const WS_VISIBLE = 268435456
const WS_MINIMIZE = 536870912
const WS_CHILD = 1073741824
const WS_CHILDWINDOW = 1073741824
# 0x80000000 as a signed 32-bit style word — the same bits WS_POPUP is.
const WS_POPUP = -2147483648
# WS_OVERLAPPED | CAPTION | SYSMENU | THICKFRAME | MINIMIZEBOX | MAXIMIZEBOX
const WS_OVERLAPPEDWINDOW = 13565952
# WS_POPUP | WS_BORDER | WS_SYSMENU, as a signed 32-bit word
const WS_POPUPWINDOW = -2138570752

const WS_EX_LEFT = 0
const WS_EX_DLGMODALFRAME = 1
const WS_EX_NOPARENTNOTIFY = 4
const WS_EX_TOPMOST = 8
const WS_EX_ACCEPTFILES = 16
const WS_EX_TRANSPARENT = 32
const WS_EX_MDICHILD = 64
const WS_EX_TOOLWINDOW = 128
const WS_EX_WINDOWEDGE = 256
const WS_EX_CLIENTEDGE = 512
const WS_EX_CONTEXTHELP = 1024
const WS_EX_RIGHT = 4096
const WS_EX_OVERLAPPEDWINDOW = 768
const WS_EX_LAYERED = 524288
const WS_EX_APPWINDOW = 262144
const WS_EX_COMPOSITED = 33554432
const WS_EX_NOACTIVATE = 134217728

# ----------------------------------------------------------------- CS_ values

const CS_VREDRAW = 1
const CS_HREDRAW = 2
const CS_DBLCLKS = 8
const CS_OWNDC = 32
const CS_CLASSDC = 64
const CS_PARENTDC = 128
const CS_NOCLOSE = 512
const CS_SAVEBITS = 2048
const CS_BYTEALIGNCLIENT = 4096
const CS_BYTEALIGNWINDOW = 8192
const CS_GLOBALCLASS = 16384
const CS_DROPSHADOW = 131072

# ----------------------------------------------------------------- MB_ values

const MB_OK = 0
const MB_OKCANCEL = 1
const MB_ABORTRETRYIGNORE = 2
const MB_YESNOCANCEL = 3
const MB_YESNO = 4
const MB_RETRYCANCEL = 5
const MB_ICONERROR = 16
const MB_ICONHAND = 16
const MB_ICONSTOP = 16
const MB_ICONQUESTION = 32
const MB_ICONWARNING = 48
const MB_ICONEXCLAMATION = 48
const MB_ICONINFORMATION = 64
const MB_ICONASTERISK = 64
const MB_DEFBUTTON1 = 0
const MB_DEFBUTTON2 = 256
const MB_DEFBUTTON3 = 512
const MB_APPLMODAL = 0
const MB_SYSTEMMODAL = 4096
const MB_TASKMODAL = 8192
const MB_SETFOREGROUND = 65536
const MB_TOPMOST = 262144

const IDOK = 1
const IDCANCEL = 2
const IDABORT = 3
const IDRETRY = 4
const IDIGNORE = 5
const IDYES = 6
const IDNO = 7
const IDCLOSE = 8

# ----------------------------------------------------------------- SW_ values

const SW_HIDE = 0
const SW_SHOWNORMAL = 1
const SW_NORMAL = 1
const SW_SHOWMINIMIZED = 2
const SW_SHOWMAXIMIZED = 3
const SW_MAXIMIZE = 3
const SW_SHOWNOACTIVATE = 4
const SW_SHOW = 5
const SW_MINIMIZE = 6
const SW_SHOWMINNOACTIVE = 7
const SW_SHOWNA = 8
const SW_RESTORE = 9
const SW_SHOWDEFAULT = 10

# CW_USEDEFAULT is C's ((int)0x80000000): "you choose" for a position or a
# size in CreateWindowExA.
const CW_USEDEFAULT = -2147483648

# ------------------------------------------------------------ SetWindowPos

const SWP_NOSIZE = 1
const SWP_NOMOVE = 2
const SWP_NOZORDER = 4
const SWP_NOREDRAW = 8
const SWP_NOACTIVATE = 16
const SWP_FRAMECHANGED = 32
const SWP_SHOWWINDOW = 64
const SWP_HIDEWINDOW = 128
const SWP_NOCOPYBITS = 256
const SWP_NOOWNERZORDER = 512
const SWP_NOSENDCHANGING = 1024

# The HWND_* pseudo-handles: numbers, so `ptr_from_int(HWND_TOPMOST)`.
const HWND_TOP = 0
const HWND_BOTTOM = 1
const HWND_TOPMOST = -1
const HWND_NOTOPMOST = -2
# A message-only window: it never draws, and it receives messages alone.
const HWND_MESSAGE = -3
const HWND_BROADCAST = 65535

# --------------------------------------------------- window words and hooks

const GWL_WNDPROC = -4
const GWL_HINSTANCE = -6
const GWL_HWNDPARENT = -8
const GWL_STYLE = -16
const GWL_EXSTYLE = -20
const GWL_USERDATA = -21
const GWL_ID = -12
const GWLP_WNDPROC = -4
const GWLP_HINSTANCE = -6
const GWLP_HWNDPARENT = -8
const GWLP_USERDATA = -21
const GWLP_ID = -12
const GCLP_HBRBACKGROUND = -10

const WH_MSGFILTER = -1
const WH_JOURNALRECORD = 0
const WH_JOURNALPLAYBACK = 1
const WH_KEYBOARD = 2
const WH_GETMESSAGE = 3
const WH_CALLWNDPROC = 4
const WH_CBT = 5
const WH_SYSMSGFILTER = 6
const WH_MOUSE = 7
const WH_DEBUG = 9
const WH_SHELL = 10
const WH_FOREGROUNDIDLE = 11
const WH_CALLWNDPROCRET = 12
const WH_KEYBOARD_LL = 13
const WH_MOUSE_LL = 14
const HC_ACTION = 0
const HC_NOREMOVE = 3

const PM_NOREMOVE = 0
const PM_REMOVE = 1
const PM_NOYIELD = 2

# --------------------------------------------------- cursors, icons, colours

const IDC_ARROW = 32512
const IDC_IBEAM = 32513
const IDC_WAIT = 32514
const IDC_CROSS = 32515
const IDC_UPARROW = 32516
const IDC_SIZENWSE = 32642
const IDC_SIZENESW = 32643
const IDC_SIZEWE = 32644
const IDC_SIZENS = 32645
const IDC_SIZEALL = 32646
const IDC_NO = 32648
const IDC_HAND = 32649
const IDC_APPSTARTING = 32650
const IDC_HELP = 32651

const IDI_APPLICATION = 32512
const IDI_HAND = 32513
const IDI_ERROR = 32513
const IDI_QUESTION = 32514
const IDI_EXCLAMATION = 32515
const IDI_WARNING = 32515
const IDI_ASTERISK = 32516
const IDI_INFORMATION = 32516
const IDI_WINLOGO = 32517
const IDI_SHIELD = 32518

const IMAGE_BITMAP = 0
const IMAGE_ICON = 1
const IMAGE_CURSOR = 2
const LR_DEFAULTCOLOR = 0
const LR_MONOCHROME = 1
const LR_LOADFROMFILE = 16
const LR_LOADTRANSPARENT = 32
const LR_DEFAULTSIZE = 64
const LR_CREATEDIBSECTION = 8192
const LR_SHARED = 32768

# A class background brush is the colour index plus one, turned into a handle:
# `ptr_from_int(int_to_int64(COLOR_WINDOW + 1))`.
const COLOR_SCROLLBAR = 0
const COLOR_BACKGROUND = 1
const COLOR_DESKTOP = 1
const COLOR_ACTIVECAPTION = 2
const COLOR_INACTIVECAPTION = 3
const COLOR_MENU = 4
const COLOR_WINDOW = 5
const COLOR_WINDOWFRAME = 6
const COLOR_MENUTEXT = 7
const COLOR_WINDOWTEXT = 8
const COLOR_CAPTIONTEXT = 9
const COLOR_ACTIVEBORDER = 10
const COLOR_INACTIVEBORDER = 11
const COLOR_APPWORKSPACE = 12
const COLOR_HIGHLIGHT = 13
const COLOR_HIGHLIGHTTEXT = 14
const COLOR_BTNFACE = 15
const COLOR_BTNSHADOW = 16
const COLOR_GRAYTEXT = 17
const COLOR_BTNTEXT = 18
const COLOR_INACTIVECAPTIONTEXT = 19
const COLOR_BTNHIGHLIGHT = 20

# ----------------------------------------------------------------- SM_ values

const SM_CXSCREEN = 0
const SM_CYSCREEN = 1
const SM_CXVSCROLL = 2
const SM_CYHSCROLL = 3
const SM_CYCAPTION = 4
const SM_CXBORDER = 5
const SM_CYBORDER = 6
const SM_CXICON = 11
const SM_CYICON = 12
const SM_CXCURSOR = 13
const SM_CYCURSOR = 14
const SM_CYMENU = 15
const SM_CXFULLSCREEN = 16
const SM_CYFULLSCREEN = 17
const SM_MOUSEPRESENT = 19
const SM_CYVSCROLL = 20
const SM_CXHSCROLL = 21
const SM_SWAPBUTTON = 23
const SM_CXMIN = 28
const SM_CYMIN = 29
const SM_CXSIZE = 30
const SM_CYSIZE = 31
const SM_CXFRAME = 32
const SM_CYFRAME = 33
const SM_CXMINTRACK = 34
const SM_CYMINTRACK = 35
const SM_CXDOUBLECLK = 36
const SM_CYDOUBLECLK = 37
const SM_CMOUSEBUTTONS = 43
const SM_CXMAXIMIZED = 61
const SM_CYMAXIMIZED = 62
const SM_XVIRTUALSCREEN = 76
const SM_YVIRTUALSCREEN = 77
const SM_CXVIRTUALSCREEN = 78
const SM_CYVIRTUALSCREEN = 79
const SM_CMONITORS = 80
const SM_REMOTESESSION = 4096

# ----------------------------------------------------------------- VK_ values

const VK_LBUTTON = 1
const VK_RBUTTON = 2
const VK_CANCEL = 3
const VK_MBUTTON = 4
const VK_BACK = 8
const VK_TAB = 9
const VK_CLEAR = 12
const VK_RETURN = 13
const VK_SHIFT = 16
const VK_CONTROL = 17
const VK_MENU = 18
const VK_PAUSE = 19
const VK_CAPITAL = 20
const VK_ESCAPE = 27
const VK_SPACE = 32
const VK_PRIOR = 33
const VK_NEXT = 34
const VK_END = 35
const VK_HOME = 36
const VK_LEFT = 37
const VK_UP = 38
const VK_RIGHT = 39
const VK_DOWN = 40
const VK_SNAPSHOT = 44
const VK_INSERT = 45
const VK_DELETE = 46
const VK_0 = 48
const VK_9 = 57
const VK_A = 65
const VK_Z = 90
const VK_LWIN = 91
const VK_RWIN = 92
const VK_APPS = 93
const VK_NUMPAD0 = 96
const VK_NUMPAD9 = 105
const VK_MULTIPLY = 106
const VK_ADD = 107
const VK_SUBTRACT = 109
const VK_DECIMAL = 110
const VK_DIVIDE = 111
const VK_F1 = 112
const VK_F2 = 113
const VK_F3 = 114
const VK_F4 = 115
const VK_F5 = 116
const VK_F6 = 117
const VK_F7 = 118
const VK_F8 = 119
const VK_F9 = 120
const VK_F10 = 121
const VK_F11 = 122
const VK_F12 = 123
const VK_NUMLOCK = 144
const VK_SCROLL = 145
const VK_LSHIFT = 160
const VK_RSHIFT = 161
const VK_LCONTROL = 162
const VK_RCONTROL = 163
const VK_LMENU = 164
const VK_RMENU = 165

# The "down" bit of a GetAsyncKeyState/GetKeyState answer, and the "toggled"
# bit of a GetKeyState answer.
const KEY_DOWN_BIT = 32768
const KEY_TOGGLED_BIT = 1

# ------------------------------------------------------- hotkeys and input

const MOD_ALT = 1
const MOD_CONTROL = 2
const MOD_SHIFT = 4
const MOD_WIN = 8
const MOD_NOREPEAT = 16384

const INPUT_MOUSE = 0
const INPUT_KEYBOARD = 1
const INPUT_HARDWARE = 2
const KEYEVENTF_EXTENDEDKEY = 1
const KEYEVENTF_KEYUP = 2
const KEYEVENTF_UNICODE = 4
const KEYEVENTF_SCANCODE = 8
const MOUSEEVENTF_MOVE = 1
const MOUSEEVENTF_LEFTDOWN = 2
const MOUSEEVENTF_LEFTUP = 4
const MOUSEEVENTF_RIGHTDOWN = 8
const MOUSEEVENTF_RIGHTUP = 16
const MOUSEEVENTF_MIDDLEDOWN = 32
const MOUSEEVENTF_MIDDLEUP = 64
const MOUSEEVENTF_WHEEL = 2048
const MOUSEEVENTF_ABSOLUTE = 32768

const MAPVK_VK_TO_VSC = 0
const MAPVK_VSC_TO_VK = 1
const MAPVK_VK_TO_CHAR = 2

# ------------------------------------------------------- clipboard formats

const CF_TEXT = 1
const CF_BITMAP = 2
const CF_METAFILEPICT = 3
const CF_SYLK = 4
const CF_DIF = 5
const CF_TIFF = 6
const CF_OEMTEXT = 7
const CF_DIB = 8
const CF_PALETTE = 9
const CF_PENDATA = 10
const CF_RIFF = 11
const CF_WAVE = 12
const CF_UNICODETEXT = 13
const CF_ENHMETAFILE = 14
const CF_HDROP = 15
const CF_LOCALE = 16
const CF_DIBV5 = 17

# --------------------------------------------------------- menus and DrawText

const MF_STRING = 0
const MF_ENABLED = 0
const MF_UNCHECKED = 0
const MF_GRAYED = 1
const MF_DISABLED = 2
const MF_CHECKED = 8
const MF_POPUP = 16
const MF_MENUBARBREAK = 32
const MF_SEPARATOR = 2048
const MF_BYCOMMAND = 0
const MF_BYPOSITION = 1024

const TPM_LEFTALIGN = 0
const TPM_CENTERALIGN = 4
const TPM_RIGHTALIGN = 8
const TPM_LEFTBUTTON = 0
const TPM_RIGHTBUTTON = 2
const TPM_RETURNCMD = 256

const DT_LEFT = 0
const DT_CENTER = 1
const DT_RIGHT = 2
const DT_VCENTER = 4
const DT_BOTTOM = 8
const DT_WORDBREAK = 16
const DT_SINGLELINE = 32
const DT_EXPANDTABS = 64
const DT_NOCLIP = 256
const DT_CALCRECT = 1024

# --------------------------------------------------------- system commands

const SC_SIZE = 61440
const SC_MOVE = 61456
const SC_MINIMIZE = 61472
const SC_MAXIMIZE = 61488
const SC_CLOSE = 61536
const SC_RESTORE = 61728

# GetWindow's cmd
const GW_HWNDFIRST = 0
const GW_HWNDLAST = 1
const GW_HWNDNEXT = 2
const GW_HWNDPREV = 3
const GW_OWNER = 4
const GW_CHILD = 5

# SystemParametersInfo actions a program reaches for most often
const SPI_GETWORKAREA = 48
const SPI_SETDESKWALLPAPER = 20
const SPI_GETSCREENSAVEACTIVE = 16
const SPI_SETSCREENSAVEACTIVE = 17
