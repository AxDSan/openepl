<!-- Generated by tools/gen-docs.sh — do not edit; edit the toolchain. -->

# Project templates

`openepl new <template> <directory>` creates a project from one of these. Each
one compiles and runs as it stands.

| Template | Builds | What you get |
| --- | --- | --- |
| `console-app` | `console` | A terminal program. Starts at `main`, prints, and does arithmetic. |
| `gui-app` | `gui` | A window with a label and a button, and a handler wired to its click. |
| `shared-library` | `sharedlib` | A .so/.dll/.dylib. Exports its subroutines for a host to call. |
| `static-library` | `staticlib` | A .a/.lib archive, linked directly into a host program. |
